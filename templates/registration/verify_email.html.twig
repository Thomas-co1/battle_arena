{% extends 'base.html.twig' %}

{% block title %}Vérification d'email - Battle Arena{% endblock %}

{% block content %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="alert alert-info" role="alert">
                    <h4 class="alert-heading">⚔️ Email en attente de vérification</h4>
                    <hr>
                    <p>Un email de confirmation a été envoyé à <strong>{{ email }}</strong>.</p>
                    <p>Cliquez sur le lien dans l'email pour activer votre compte.</p>
                    <p class="mb-0">Une fois l'email confirmé, vous pourrez accéder à toutes les fonctionnalités de Battle Arena.</p>
                </div>

                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Besoin d'aide?</h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Vous n'avez pas reçu l'email?</strong></p>
                        <ul>
                            <li>Vérifiez votre dossier spam</li>
                            <li>Vérifiez l'adresse email saisie</li>
                        </ul>
                        <form method="POST" action="{{ path('app_register') }}">
                            <button type="submit" class="btn btn-primary">Réessayer l'inscription</button>
                        </form>
                    </div>
                </div>

                <div class="mt-4 text-center">
                    <a href="{{ path('app_logout') }}" class="btn btn-secondary">Se déconnecter</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
